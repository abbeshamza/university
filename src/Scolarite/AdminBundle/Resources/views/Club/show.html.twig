{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Club</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ entity.nom }}</td>
            </tr>
            <tr>
                <th>President</th>
                <td>{{ entity.president }}</td>
            </tr>
            <tr>
                <th>Nbrmembre</th>
                <td>{{ entity.nbrMembre }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('club') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('club_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
