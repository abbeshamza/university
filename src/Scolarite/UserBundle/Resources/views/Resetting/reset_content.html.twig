{#<form action="{{ path('fos_user_resetting_reset', {'token': token}) }}" {{ form_enctype(form) }} method="POST" class="fos_user_resetting_reset">
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'resetting.reset.submit'|trans({}, 'FOSUserBundle') }}" />
    </div>
</form>#}


<form class="form-signin" action="{{ path('fos_user_resetting_reset', {'token': token}) }}" method="POST" class="fos_user_resetting_reset">
    <h2 class="form-signin-heading">Réinitialiser le mot de passe</h2>
    <div class="login-wrap">
        {#{% if invalid_username is defined %}
            <div class="alert alert-danger fade in">{{ 'resetting.request.invalid_username'|trans({'%username%': invalid_username}) }}
                <button type="button" class="close" data-dismiss="alert">
                    <i class="icon-remove"></i>
                </button>
            </div>
        {% endif %}#}
        <div class="user-login-info">

            {#            {{ form_widget(form) }}#}

            {{ form_errors(form.new.first) }}
            {{ form_widget(form.new.first, {'attr': {'class' : 'form-control', 'placeholder' : 'New password'} }) }}
            {{ form_errors(form.new.second) }}
            {{ form_widget(form.new.second, {'attr': {'class' : 'form-control', 'placeholder' : 'Verification'} }) }}
            {{ form_rest(form) }}
            {{ form_end(form) }}
        </div>

        <button type="submit" class="btn btn-lg btn-login btn-block">
            <i class="icon-lightbulb"></i>
            {{ 'resetting.reset.submit'|trans({}, 'FOSUserBundle') }}
        </button>
        <div class="registration">
            <a class="" href="{{ path('fos_user_security_login') }}">
                Retour au formulaire de connexion
            </a>
        </div>
    </div>
</form>
